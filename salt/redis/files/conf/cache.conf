{%- set maxmemory = [64, [512, (ram_ratio|default(0.05) * grains.mem_total) | int] | min] | max -%}
maxmemory {{ maxmemory }}mb
maxmemory-policy {{ maxmemory_policy|default('allkeys-lru') }}
unixsocket /run/redis/redis.sock
unixsocketperm 660
appendonly no
save ""

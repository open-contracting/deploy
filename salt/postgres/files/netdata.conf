{%- for database in salt['pillar.get']('postgres:databases', {}) %}
{{ database }}:
  name         : '{{ database }}'
  user         : 'netdata'
  database     : '{{ database }}'
  host         : 'localhost'
  password:    : '{{ pillar.netdata.postgres }}'
  port         :  5432
{%- endfor %}

{% raw %}
groups:
- name: alert.rules
  rules:
  - alert: InstanceDown
    expr: up == 0
    for: 3m
    labels:
      severity: "critical"
    annotations:
      summary: "Endpoint {{ $labels.instance }} down"
      description: "{{ $labels.instance }} of job {{ $labels.job }} is down."
{% endraw %}
